services:
  visidiff:
    build:
      context: .
      target: dev
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - runs:/app/public/runs
    extra_hosts:
      - "localhost:host-gateway"
    environment:
      - HOST=0.0.0.0
